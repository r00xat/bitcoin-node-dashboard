{
   "name": "bitcoin-node-dashboard",
   "version": "0.0.1",
   "author": "MontejoJorge",
   "workspaces": [
      "fronend",
      "backend"
   ],
   "scripts": {
      "dev:docker": "docker compose -f docker-compose.dev.yml up --build --force-recreate --no-deps",
      "build:dev:docker": "docker compose -f docker-compose.dev.yml build --no-cache",
      "prettier": "npx prettier --write ."
   },
   "prettier": {
      "trailingComma": "es5",
      "tabWidth": 3,
      "semi": true,
      "singleQuote": true
   },
   "devDependencies": {
      "prettier": "^3.1.1"
   }
}
